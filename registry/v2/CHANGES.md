# Channel v2 Changelog

Channel v2 opens when the first breaking change to a v1 component is required. Until then this is a placeholder.

## Migration codemods

Each breaking change shipped to v2 MUST include a `shadcn migrate <name>` codemod under `scripts/codemods/`. Pattern template: `scripts/migrate-codemod-template.ts`.
